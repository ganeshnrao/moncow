export default `// moncow

const _ = require("lodash");
const Promise = require("bluebird");
const moment = require("moment");

module.exports = async function() {
  // use \`connect(environment, dbNames)\` to connect to
  // databases specified in \`moncow.connections\`
  // write your code here!
  return "Returned value will be displayed"
};
`;
